# 데이터 (캐시 DB는 재생성 가능 — git 추적 제외)
#data/
data/uqi_cache.db
# 미사용 placeholder (어떤 코드도 참조하지 않음). 재생성되어도 ignore.
data/uqi_knowledge.db

# 로그
logs/

# 환경변수
.env
# 주의: .env.gpg (암호화 백업본) 은 repo 에 추적 — passphrase 가 유일 방어선이라
# 강한 passphrase 필수. private repo 가정.

# Python
**/__pycache__/
*.pyc
*.pyo
*.pyd

# venv
.venv_transpile/
.venv/
venv/

# IDE
.vscode/
.idea/

# pytest
.pytest_cache/
.coverage
node_modules/

# claude desktop / mcp workspaces
.claude/

# Notion backup static site (Quartz build output, rsync'd from separate project)
# — content is generated; should not be tracked in UQI repo
webapp/notion-backup/
webapp/notion-backup
