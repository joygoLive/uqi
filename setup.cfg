[tool:pytest]
pythonpath = src webapp
testpaths = tests
# --forked: 각 테스트를 별도 프로세스에서 실행 (test isolation 보장)
# 빠른 실행을 원할 때만 명시적으로 -p no:forked 사용
addopts = --forked

[coverage:run]
source = .

[coverage:report]
omit =
    tests/*
    ../QUWA/.venv_transpile/*
